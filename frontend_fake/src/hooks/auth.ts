// import {useTokenStore} from "../store/auth.ts";
import {useEffect} from "react";

export const useTokenInit = function (){
    // const setToken = useTokenStore((s)=>{return s.setAccessToken})
    // const token = useTokenStore((s)=>{return s.accessToken})

    useEffect(()=>{

    },[])
}